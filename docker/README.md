# Docker

- `docker-compose.yml` — Postgres + backend (FastAPI). Опционально фронт в dev.
- `Dockerfile.backend` — сборка FastAPI.
- `Dockerfile.pwa` — сборка SvelteKit (если нужно рантайм-развёртывание).
- Скрипты/entrypoint класть сюда же.

