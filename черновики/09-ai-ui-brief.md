# AI UI Brief для генеративного дизайна

Документ описывает, какое приложение строим и что должно быть на ключевых экранах. Его можно использовать как ТЗ для AI, чтобы отрисовать макеты, согласованные с продуктовой логикой PickPock.

## 1. Что это за приложение
- **PickPock** — личный «карманный дневник» жизненных событий и трат. Базовая сущность: `Entry = Action + Object + Context`.
- Пользователь фиксирует впечатления, финансы, места, людей и эмоции. AI помогает распознать смысл, заполнить поля, найти паттерны, вести диалог.
- Форм-фактор: **PWA, mobile-first** (портрет 390×844 как baseline), но адаптив к планшетам/desktop за счёт левых навигационных панелей.
- Визуальный язык: нейтральный светлый фон, акцентный primary (#111/темный), мягкие карточки, шрифт sans (Inter). Компоненты — shadcn-svelte.
- Тон голоса интерфейса — дружелюбный, но информативный; AI должен пояснять логику ответов и показывать уверенность.

## 2. Общие принципы UI
1. **Карточная сетка**: каждая смысловая группа — отдельная карточка с заголовком и вторичным текстом. В списках используем компактные карточки с кнопками действий.
2. **Sticky actions**: на мобильных — плавающая кнопка «+» (новая запись); на desktop — вторичные CTA в хедере (AI чат, Черновики).
3. **Навигация**: боковое меню (desktop) + вкладки/кнопки в верхнем баре (mobile). Основные разделы: Dashboard, Entries, New, Search, Bot, Settings, Onboarding.
4. **Состояния**: для каждого экрана прописываем empty/loading/filled. AI-экраны дополнительно показывают confidence, источники, action buttons.
5. **Мультимодальность**: в формах есть кнопки ввода текста, голоса, фото, ссылок; AI может предложить распознанные поля и уточнения.

## 3. Экраны и ожидания

### 3.1 Onboarding (`/onboarding`)
- **Цель**: быстро объяснить ценность и настроить контекст (чем занимался пользователь).
- **Структура**: горизонтальный pager 3–4 карточек, каждая с иллюстрацией и коротким описанием; прогресс-индикатор. В конце — кнопка «Начать», переключатель «Импорт из Google/Telegram» (пока disabled).
- **Состояния**: первая загрузка (CTA подсвечен), повторный визит — показать кнопку «Перейти в приложение».

### 3.2 Dashboard (`/`)
- **Цель**: дать сводку последней активности, быстрые действия.
- **Блоки**:
	- Hero с приветствием, кнопками «Новая запись», «AI чат».
	- «Последние записи» (3 карточки с эмоцией/суммой/geo).
	- «Фокусы недели»: чипы (например, «эмоция wow 3×», «транспорт +28%»).
	- «Синхронизация» (статус локальных драфтов, очередь AI).
- **Особенности**: показывать фавиконы людей/мест (initials), указывать confidence для AI подсказок.

### 3.3 Entries (`/entries`)
- **Цель**: лента всех записей с фильтрами.
- **Блоки**:
	- Sticky фильтр-бар (период, категории, эмоции, текст-поиск).
	- Список карточек: заголовок (Action + Object), метаданные (дата, сумма, теги), кнопки «Открыть», «Редактировать».
	- Empty state с предложением создать запись или попросить AI импортировать.
- **Дополнительно**: на desktop рядом можно показать мини-карту или статистику выбранного фильтра.

### 3.4 Entry Detail (`/entry/[id]`)
- **Цель**: показать полную карточку Entry.
- **Блоки**:
	- Header с иконкой категории, суммой, эмоцией, кнопками «Редактировать», «Поделиться».
	- Секции: Описание, Финансы, Локация, Люди, Файлы/фото, AI заметки.
	- Timeline изменений/AI подсказок внизу.
- **Состояния**: для пустых секций показываем кнопки «добавить из AI»/«добавить вручную».

### 3.5 New Entry Wizard (`/new`)
- **Цель**: 2-шаговый визард, поддерживающий текст/фото/URL.
- **Шаг 1 — Capture (ввод)**:
	- Блок фото с возможностью загрузки или съемки с камеры, кнопка «+» и несколько слотов (например, 3 квадрата: один активный, остальные — контуры).
	- Произвольный текстовый блок (можно вставлять любой текст/ссылки или оставить пустым).
	- Кнопка «Говорить голосом» — диктовка заполняет текстовый блок.
	- CTA «Распознать с ИИ».
- **Шаг 2 — AI Draft + Review (подтверждение)**:
	- AI парсит запись: категория, подкатегория, все доступные поля, confidence.
	- AI формирует структурированное описание, например:
		```
		[Описание]
		Текст

		[Фото 1]
		Текст

		Processed — 92% confidence
		```
	- Поля разделены на обязательные, обязательные по категории/подкатегории, рекомендуемые и дополнительные.
	- Поля редактируемые (input или иконка ✏️), можно очистить/удалить.
	- Контурный блок «Добавить поле» открывает модалку выбора поля и заполнения значения.
	- Интерактивные поля (например, выбор эмоции: 🙂 Happy / 😐 Neutral / 😔 Sad / 🎉 Excited).
	- Кнопка «Сохранить».
- **Навигация**: верхний прогресс-бар, кнопки «Назад/Далее», возможность свернуть в bottom-sheet.

### 3.6 AI Search (`/search`)
- **Цель**: семантический поиск по базе.
- **Блоки**:
	- Поле свободного ввода + кнопки «Голос» и «Искать».
	- «AI распознал» — чипы с фильтрами, которые система построила.
	- Список карточек результатов (название, дата, сумма, теги, CTA «Открыть», «Сохранить фильтр»).
	- Боковая панель «Контекст запроса» (что учли, что можно сделать, альтернативы).
	- История запросов и блок подсказок.
- **Особенности**: отображать confidence, количество записей, действия «Отправить в чат-бот», «Экспортировать».

### 3.7 AI Bot (`/bot`)
- **Цель**: диалог с помощником, который держит цепочку.
- **Блоки**:
	- История сообщений (пузырьки «Ты»/«PickPock AI», бейджи фильтров, кнопки действий в ответе).
	- Поле ввода + кнопки «Голос», «Отправить», подсказки-чипы.
	- Карточки справа: «Как бот помогает», «Состояние сессии» (контекст, источники, тон), «Команды» (/summarize, /compare, /handoff).
- **Особенности**: в каждом ответе AI указывать источники (ID записей), confidence, быстрые CTA («Открыть записи», «Сохранить фильтр»).

### 3.8 Settings & Recipes (`/settings`)
- **Цель**: управление аккаунтом, словарями, интеграциями.
- **Блоки**:
	- Профиль (аватар, имя, email, кнопка «Синхронизация»).
	- «Recipes & Templates»: список рецептов, кнопки «добавить», теги, статус публикации.
	- Интеграции (Telegram, Notion, Google Calendar) — карточки с toggle + статус.
	- Напоминания/уведомления (расписание, тихие часы).
- **Особенности**: разделы оформлять как аккордеоны; показывать предупреждения, если синхронизация отключена.

### 3.9 Системные элементы
- **AppShell**: верхний бар (leading action + title + CTA), левый сайдбар с навигацией, плавающая кнопка на мобильных.
- **Списки/таблицы**: использовать иконки категорий, теги-значки, индикаторы эмоций (цветные точки).
- **Диалоги**: подтверждения удаления, сохранения фильтров, переключение AI-тонов.

## 4. Что отдать генеративному ИИ
1. Этот документ (описание приложения и экранов).
2. Примерное разрешение (mobile first), цветовая палитра (светлая тема, primary #111, secondary #f3f4f6, accent #4f46e5).
3. Требование использовать карточный layout, шрифт Inter, иконки Lucide.

> Если AI предлагает вариации, держим единый стиль: мягкие скругления (16px), тени уровня 1–2, достаточные отступы (16/24px).
