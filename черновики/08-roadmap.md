# Roadmap

> TL;DR: таблица ниже = «что построили → что делаем → что потом». Каждый этап = законченный кусок продукта. Открываем документ в любой момент и моментально понимаем, где мы.

| Этап | Статус | Коротко | Что входит |
|------|--------|---------|------------|
| **G0 — База** | ✅ | Собрали фундамент | - Monorepo структура (`frontend/`, `backend/`, `common/`, `docker/`).<br>- Docker (Postgres/Redis/backend), `.env` шаблоны.<br>- Alembic + модель `entries`.<br>- `/api/v1/config/entry-schema`, генерация типов, базовый README. |
| **G1 — Tests, Migrations & CI** | ✅ | Фокус на стабилизации | - CRUD `/api/v1/entries` завершён.<br>- `poetry run alembic upgrade head` во всех окружениях.<br>- Pydantic модели из `common/schemas`.<br>- pytest/httpx, фикстуры под будущий sync.<br>- CI для lint/tests/migrations. |
| **G2 — Frontend Integration** | 🚧 | В работе | - Фронт тянет схемы с backend вместо локальных файлов.<br>- IndexedDB слой: draft cache, conflict flags, optimistic UI.<br>- UI ленты/деталей/формы (shadcn-svelte).<br>- E2E цепочка «создал запись → backend → перезагрузка → вижу запись». |
| **G3 — Sync & Background** | 🔜 | После интеграции | - `/api/v1/sync/state|push|pull` (версии, курсоры, конфликт-резолюция).<br>- Celery worker + Redis очередь, задачи обработки вложений/AI.<br>- Новые таблицы `sync_log`, `entry_files` и др.<br>- e2e сценарий offline→online. |
| **G4 — Observability & Security** | 🔜 | Надёжность | - Structured logs (loguru + correlation-id).<br>- Health-check Postgres/Redis/Celery, метрики, alert hooks.<br>- Auth baseline (JWT/session, spaces), rate limiting, audit лог.<br>- Data quality проверки (Alembic/SQL). |
| **G5 — AI & Recipes** | 🔜 | Интеллект + справочники | - Подключение OpenRouter (мульти-модели).<br>- `/api/v1/recipes` + редактор/версионность.<br>- Рекомендации, semantic search/analytics, интеграции (Google Calendar и т.п.).<br>- Кеширование конфигов для оффлайна. |

### Дальше горизонты (G6+)
- **G6 — Упаковка и automation**: Capacitor/desktop билды, release pipeline, auto-versioning, инфраструктурные тесты.
- **G7 — Monetization & Sharing**: платные планы (квоты AI/пространств), sharing/collaboration, экспорт/backup сервис.

## Контроль
- Каждый переход между этапами отражаем в `docs/05-log.md`.
- При корректировке приоритетов обновляем `docs/02-plan.md`.
- После завершения G1 включаем CI пайплайн (lint + tests + docker build).
