# Улучшенная система намерений и маппинга

## 🎯 Проблема текущей системы

1. **Намерения маппятся слишком грубо:**
   - `purchase` → `bought` (но это может быть и `spent` для чека)
   - `transfer` → `spent` (но это может быть и `received`)
   - `want_buy` → `planned` (но object не определен)

2. **Намерение не используется после AI анализа:**
   - AI возвращает `action + object`
   - Но намерение пользователя игнорируется при поиске рецепта
   - Нет приоритизации рецептов на основе намерения

3. **Нет связи намерение → возможные рецепты:**
   - Не показываем пользователю, какие рецепты возможны для его намерения
   - Не фильтруем рецепты по намерению

---

## ✅ Новая концепция

### 1. Намерения как предсказание semantic (action + object)

Вместо маппинга намерения → action, маппим на **action + object**:

```typescript
interface IntentMapping {
  intent: PhotoAction | UrlAction;
  // Возможные комбинации action + object для этого намерения
  possibleSemantics: Array<{
    action: Action;
    object: Object;
    priority: number; // 1-10, выше = более вероятно
  }>;
  // Описание для AI
  aiHint: string;
}
```

### 2. Использование намерения для поиска рецепта

После AI анализа:
1. Если AI вернул `action + object` → ищем рецепт
2. Если рецепт не найден → используем намерение для **предсказания** возможных рецептов
3. Показываем рецепты, которые соответствуют намерению

### 3. Умный маппинг намерений

```typescript
const INTENT_MAPPINGS: Record<string, IntentMapping> = {
  // ФОТО
  purchase: {
    possibleSemantics: [
      { action: 'bought', object: 'item', priority: 9 },
      { action: 'spent', object: 'money', priority: 8 },
      { action: 'bought', object: 'service', priority: 7 }
    ],
    aiHint: 'покупка/оплата (чек, товар, услуга)'
  },
  transfer: {
    possibleSemantics: [
      { action: 'spent', object: 'money', priority: 9 },
      { action: 'received', object: 'money', priority: 8 },
      { action: 'lent', object: 'money', priority: 7 }
    ],
    aiHint: 'перевод денег другому человеку (скрин банка, долг)'
  },
  want_buy: {
    possibleSemantics: [
      { action: 'planned', object: 'item', priority: 9 },
      { action: 'planned', object: 'service', priority: 7 }
    ],
    aiHint: 'планы/желания (хочу купить)'
  },
  want_go: {
    possibleSemantics: [
      { action: 'planned', object: 'place', priority: 9 },
      { action: 'planned', object: 'wish', priority: 7 }
    ],
    aiHint: 'планы/желания (хочу пойти)'
  },
  liked: {
    possibleSemantics: [
      { action: 'noted', object: 'food', priority: 8 },
      { action: 'noted', object: 'place', priority: 8 },
      { action: 'noted', object: 'item', priority: 7 },
      { action: 'noted', object: 'note', priority: 6 } // может быть experience
    ],
    aiHint: 'опыт/впечатление (понравилось)'
  },
  document: {
    possibleSemantics: [
      { action: 'noted', object: 'note', priority: 9 },
      { action: 'noted', object: 'money', priority: 7 } // квитанция
    ],
    aiHint: 'документ для сохранения (факт или справка)'
  }
};
```

---

## 🔄 Улучшенный пайплайн

### Шаг 1: Пользователь выбирает намерение

**Для фото:**
- Покупка → предсказываем: `bought+item`, `spent+money`
- Перевод → предсказываем: `spent+money`, `received+money`
- Хочу купить → предсказываем: `planned+item`
- Хочу пойти → предсказываем: `planned+place`
- Понравилось → предсказываем: `noted+food`, `noted+place`
- Документ → предсказываем: `noted+note`

**Для URL:**
- Хочу купить → предсказываем: `planned+item`
- Хочу пойти → предсказываем: `planned+place`
- Покупка → предсказываем: `bought+item`, `spent+money`
- Понравилось → предсказываем: `noted+item`, `noted+place`
- Документ → предсказываем: `noted+note`

### Шаг 2: AI анализ с учетом намерения

AI получает:
- `hintType` → описание намерения
- `customDescription` → дополнительный контекст
- Список возможных `action + object` из намерения

AI возвращает: `{ action, object, ... }`

### Шаг 3: Умный поиск рецепта

```typescript
function findRecipeWithIntent(
  aiResult: AIAnalysisResult,
  intent: PhotoAction | UrlAction
): Recipe | null {
  // 1. Прямой поиск по AI результату
  const directRecipe = getRecipeBySemantic(aiResult.action, aiResult.object);
  if (directRecipe) return directRecipe;

  // 2. Если не найден, используем намерение для предсказания
  const intentMapping = INTENT_MAPPINGS[intent];
  if (!intentMapping) return null;

  // 3. Ищем рецепты по возможным комбинациям из намерения
  // Сортируем по приоритету
  const possibleRecipes = intentMapping.possibleSemantics
    .sort((a, b) => b.priority - a.priority)
    .map(semantic => getRecipeBySemantic(semantic.action, semantic.object))
    .filter(Boolean) as Recipe[];

  // 4. Если есть рецепты, возвращаем самый приоритетный
  if (possibleRecipes.length > 0) {
    return possibleRecipes[0];
  }

  return null;
}
```

### Шаг 4: Определение категории с учетом намерения

Если рецепт не найден, определяем категорию:

```typescript
function getCategoryWithIntent(
  aiResult: AIAnalysisResult,
  intent?: PhotoAction | UrlAction
): string {
  // 1. Прямой маппинг из object
  const categoryFromObject = getCategoryFromObject(aiResult.object, intent, aiResult.tags);
  
  // 2. Если намерение есть, используем его для уточнения
  if (intent) {
    const intentMapping = INTENT_MAPPINGS[intent];
    if (intentMapping) {
      // Берем самую приоритетную комбинацию
      const topSemantic = intentMapping.possibleSemantics[0];
      const recipe = getRecipeBySemantic(topSemantic.action, topSemantic.object);
      if (recipe?.category) {
        // Если категория из намерения более специфична, используем её
        return recipe.category;
      }
    }
  }
  
  return categoryFromObject;
}
```

---

## 📊 Преимущества

1. **Более точное предсказание:**
   - Намерение → несколько возможных `action + object`
   - Приоритизация по вероятности

2. **Умный поиск рецепта:**
   - Если AI не нашел точный рецепт → используем намерение
   - Показываем наиболее вероятный рецепт

3. **Лучшее определение категории:**
   - Учитываем намерение при определении категории
   - Более точный маппинг

4. **Расширяемость:**
   - Легко добавить новые намерения
   - Легко изменить приоритеты

---

## 🔧 Реализация

1. Создать `src/lib/config/intent-mapping.ts` с маппингами
2. Обновить `handleAISuccess` для использования намерения
3. Обновить `getCategoryFromObject` для учета намерения
4. Добавить функцию `findRecipeWithIntent`

